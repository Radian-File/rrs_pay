<UserControl x:Class="rrs_pay.Views.Pages.PayslipPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:rrs_pay.ViewModels.Pages"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1080">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Shared/PageStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.DataContext>
        <vm:PayrollDetailViewModel />
    </UserControl.DataContext>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <Border Width="620" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#FFF8FAFC" CornerRadius="12" Padding="28">
            <StackPanel>
                <DockPanel Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="RRS Pay" FontSize="24" FontWeight="Bold" Foreground="#FF0F172A" />
                        <TextBlock Text="Payslip Preview" Foreground="#FF475569" />
                    </StackPanel>
                    <StackPanel DockPanel.Dock="Right" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding PayrollNo}" Foreground="#FF0F172A" FontWeight="SemiBold" TextAlignment="Right" />
                        <TextBlock Text="{Binding Period}" Foreground="#FF475569" TextAlignment="Right" />
                    </StackPanel>
                </DockPanel>

                <Border BorderBrush="#FFE2E8F0" BorderThickness="0,1,0,1" Padding="0,14" Margin="0,0,0,18">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Employee" Foreground="#FF64748B" />
                            <TextBlock Text="{Binding EmployeeName}" Foreground="#FF0F172A" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding EmployeeCode}" Foreground="#FF475569" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Department / Position" Foreground="#FF64748B" />
                            <TextBlock Text="{Binding Department}" Foreground="#FF0F172A" FontSize="18" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Position}" Foreground="#FF475569" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,16,0">
                        <TextBlock Text="Earnings" FontWeight="SemiBold" Foreground="#FF16A34A" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding Earnings}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="0,0,0,7">
                                        <TextBlock Text="{Binding Description}" Foreground="#FF334155" />
                                        <TextBlock Text="{Binding Amount, StringFormat={}{0:N0}}" Foreground="#FF0F172A" DockPanel.Dock="Right" />
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="16,0,0,0">
                        <TextBlock Text="Deductions" FontWeight="SemiBold" Foreground="#FFD97706" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding Deductions}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <DockPanel Margin="0,0,0,7">
                                        <TextBlock Text="{Binding Description}" Foreground="#FF334155" />
                                        <TextBlock Text="{Binding Amount, StringFormat={}{0:N0}}" Foreground="#FF0F172A" DockPanel.Dock="Right" />
                                    </DockPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>

                <Border Background="#FFECFDF5" CornerRadius="10" Padding="16" Margin="0,18,0,0">
                    <DockPanel>
                        <TextBlock Text="Net Salary" FontSize="20" FontWeight="Bold" Foreground="#FF064E3B" />
                        <TextBlock Text="{Binding NetSalary, StringFormat={}{0:N0}}" FontSize="20" FontWeight="Bold" Foreground="#FF064E3B" DockPanel.Dock="Right" />
                    </DockPanel>
                </Border>
                <TextBlock Text="{Binding Notes}" Foreground="#FF64748B" TextWrapping="Wrap" Margin="0,14,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
