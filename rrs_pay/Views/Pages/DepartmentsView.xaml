<UserControl x:Class="rrs_pay.Views.Pages.DepartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:rrs_pay.ViewModels.Pages"
             mc:Ignorable="d"
             d:DesignHeight="720"
             d:DesignWidth="1080">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Shared/PageStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.DataContext>
        <vm:DepartmentsViewModel />
    </UserControl.DataContext>

    <Grid Background="{StaticResource PageBackgroundBrush}" Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="Departments" Style="{StaticResource PageTitleText}" />
            <TextBlock Text="Maintain organizational departments for employee grouping and payroll reporting." Foreground="{StaticResource SecondaryTextBrush}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,14,0">
                <StackPanel>
                    <TextBlock Text="Department Form" Style="{StaticResource SectionTitleText}" />
                    <TextBlock Text="Code" Style="{StaticResource CaptionText}" />
                    <TextBox Text="{Binding DepartmentCode, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                    <TextBlock Text="Name" Style="{StaticResource CaptionText}" />
                    <TextBox Text="{Binding DepartmentName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                    <TextBlock Text="Manager" Style="{StaticResource CaptionText}" />
                    <TextBox Text="{Binding Manager, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputTextBoxStyle}" />
                    <TextBlock Text="Description" Style="{StaticResource CaptionText}" />
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True"
                             MinHeight="90"
                             TextWrapping="Wrap"
                             Style="{StaticResource InputTextBoxStyle}" />
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Save" Command="{Binding SaveDepartmentCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                        <Button Content="Clear" Command="{Binding ClearFormCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <StackPanel>
                    <TextBlock Text="Department List" Style="{StaticResource SectionTitleText}" />
                    <DataGrid ItemsSource="{Binding Departments}" Style="{StaticResource DarkDataGridStyle}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="90" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="1.2*" />
                            <DataGridTextColumn Header="Manager" Binding="{Binding Manager}" Width="1.2*" />
                            <DataGridTextColumn Header="Employees" Binding="{Binding EmployeeCount}" Width="95" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
